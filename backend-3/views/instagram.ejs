<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram page</title>
    <style>
        img{
            width: 100px;
            height: 100px;
        }
    </style>
</head>
<body>
    <h1>This is the page of this username: <%= data.name %></h1>
<h3>Follow : <%= data.followers %></h3>
<h3>Following : <%= data.following %></h3>

<% for(let post of data.posts) {%>
<img src="<%= post.image %>" alt="some img">
<h4>Likes:<%= post.likes %> <span style="padding-left: 10px;"></span>Comments:<%= post.comments %></h4>
<% } %>
</body>
</html>